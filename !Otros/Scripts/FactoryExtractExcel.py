import pandas as pd
class extractExcel:
    # Constructor method (initialize object attributes)
    def __init__(self):
        self.df
    
    def extractData(self,fileURL):
        self.df = pd.read_excel(fileURL)
        

    def transformarDatos(self,region, _elemDict):
        loadFile = loadFile("colegios.csv", "fuente")
        return 0

    def saveHardData(self, region):
        # Se guarda data en bruto
        return 0
    
